{
  "name": "sigint-web-ui",
  "version": "0.1.0",
  "description": "SIGINT web UI — Preact + TypeScript + esbuild",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.19.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "esbuild": "^0.25.0",
    "typescript": "^5.3.3"
  }
}
