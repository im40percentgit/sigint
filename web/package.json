{
  "name": "sigint-web",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "dependencies": {
    "htm": "^3.1.1",
    "preact": "^10.19.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
