# Decision Registry
> Auto-generated by surface.sh from @decision annotations in source code.
> **DO NOT HAND-EDIT** — this file is fully regenerated each session.
> To add or change a decision, edit the @decision annotation in source code.
> Last updated: 2026-04-24 13:31

## By Component

### 4D-RECON
- **DEC-4D-RECON-001**: recon command uses best-effort persistence matching scan.rs pattern  (`crates/sigint-cli/src/recon.rs`, status: accepted)
- **DEC-4D-RECON-002**: ReconEngine borrows &Database and &EventBus — both live for the scope of run()  (`crates/sigint-cli/src/recon.rs`, status: accepted)

### 4D-STATE
- **DEC-4D-STATE-001**: Assets panel added as fifth panel in the Tab cycle  (`crates/sigint-tui/src/state.rs`, status: accepted)

### 4D-UI
- **DEC-4D-UI-001**: Findings and Assets share the bottom row as a 50/50 horizontal split  (`crates/sigint-tui/src/ui/scan.rs`, status: accepted)

### AGENT
- **DEC-AGENT-001**: Six-role pipeline with optional RfRecon prefix (DEC-AKAEI-003)  (`crates/sigint-agents/src/role.rs`, status: accepted)
- **DEC-AGENT-002**: Agent trait is synchronous — no async methods  (`crates/sigint-agents/src/agent.rs`, status: accepted)
- **DEC-AGENT-003**: TaskContext carries accumulated outputs; each agent sees all prior work  (`crates/sigint-agents/src/context.rs`, status: accepted)
- **DEC-AGENT-004**: to_agent_prompt tailors context per role rather than dumping all state  (`crates/sigint-agents/src/context.rs`, status: accepted)
- **DEC-AGENT-005**: Token estimation heuristic: text.len() / 4  (`crates/sigint-agents/src/state.rs`, status: accepted)
- **DEC-AGENT-006**: Trim to 80% of context_window, preserve all system messages  (`crates/sigint-agents/src/state.rs`, status: accepted)
- **DEC-AGENT-007**: Streaming (`chat_stream()`) for all tool-loop iterations (Phase 8A)  (`crates/sigint-agents/src/loop_engine.rs`, status: accepted)
- **DEC-AGENT-008**: Event emission is best-effort; errors are silently discarded  (`crates/sigint-agents/src/loop_engine.rs`, status: accepted)
- **DEC-AGENT-009**: Executor has full tool access: nmap, shell, gobuster, nikto, nuclei, feroxbuster, hydra, wpscan, testssl, hashcat, masscan, tshark, responder, msf_exploit, linpeas_enum, enum4linux_scan, trivy_scan, scout_suite_scan, cloudsploit_scan  (`crates/sigint-agents/src/agents/executor.rs`, status: accepted)
- **DEC-AGENT-010**: Approval gate checks tool risk before execution; blocks on oneshot channel  (`crates/sigint-agents/src/loop_engine.rs`, status: accepted)
- **DEC-AGENT-011**: ToolRegistry owns Box<dyn Tool>; for_agent returns &dyn Tool slices  (`crates/sigint-agents/src/registry.rs`, status: accepted)
- **DEC-AGENT-012**: ScanReport is a plain data struct with Display; no builder pattern  (`crates/sigint-agents/src/report.rs`, status: accepted)
- **DEC-AGENT-013**: Agents are instantiated locally inside run_scan, not stored as fields  (`crates/sigint-agents/src/orchestrator.rs`, status: accepted)
- **DEC-AGENT-014**: Orchestrator holds Arc<dyn LlmProvider> to enable cheap Clone across agent turns  (`crates/sigint-agents/src/orchestrator.rs`, status: accepted)
- **DEC-AGENT-015**: Per-tool scan record persistence is opt-in via `Option<&Database>`  (`crates/sigint-agents/src/loop_engine.rs`, status: accepted)
- **DEC-AGENT-016**: Orchestrator holds `Option<Arc<Database>>` + `session_id` for per-tool persistence  (`crates/sigint-agents/src/orchestrator.rs`, status: accepted)
- **DEC-AGENT-017**: Convergence loop uses max_cycles=1 default to preserve backward compatibility  (`crates/sigint-agents/src/orchestrator.rs`, status: accepted)
- **DEC-AGENT-018**: InteractiveSession as EventBus consumer for TUI input routing  (`crates/sigint-agents/src/interactive.rs`, status: accepted)
- **DEC-AGENT-019**: Output-only tools bypass execution registry  (`crates/sigint-agents/src/registry.rs`, status: accepted)

### AGENT-PERSIST-REDACT
- **DEC-AGENT-PERSIST-REDACT-001**: Redact credentials from tool-call args at persistence boundary  (`crates/sigint-agents/src/loop_engine.rs`, status: accepted)

### AGENT-PROMPT-SAFETY
- **DEC-AGENT-PROMPT-SAFETY-001**: Tool output is wrapped in untrusted-data delimiters before LLM ingestion  (`crates/sigint-agents/src/prompt_safety.rs`, status: accepted)
- **DEC-AGENT-PROMPT-SAFETY-002**: Case-insensitive scrub of injection markers via lowercase byte-offset matching  (`crates/sigint-agents/src/prompt_safety.rs`, status: accepted)

### AGENT-TOOL-ACL
- **DEC-AGENT-TOOL-ACL-001**: Static ACL registry enforces minimum risk for known tools  (`crates/sigint-agents/src/tool_acl.rs`, status: accepted)

### AKAEI
- **DEC-AKAEI-001**: akaei tools use tokio::process::Command directly — no sandbox  (`crates/sigint-tools/src/akaei/mod.rs`, status: accepted)
- **DEC-AKAEI-002**: audit --format json emits a JSON object; parsed directly into structured_data  (`crates/sigint-tools/src/akaei/audit.rs`, status: accepted)
- **DEC-AKAEI-003**: RfRecon runs before Researcher; skipped when akaei tools absent  (`crates/sigint-agents/src/agents/rf_recon.rs`, status: accepted)

### APPROVE
- **DEC-APPROVE-001**: std::sync::Mutex + tokio::sync::oneshot for the approval registry  (`crates/sigint-core/src/approval.rs`, status: accepted)

### ARCH
- **DEC-ARCH-001**: [untitled]  (`crates/sigint-core/src/lib.rs`, status: )
- **DEC-ARCH-002**: Cargo workspace with sigint-core as shared domain layer  (`crates/sigint-core/src/types.rs`, status: accepted)

### ASM
- **DEC-ASM-001**: Asset store uses SELECT-then-INSERT for upsert, no UPSERT SQL  (`crates/sigint-store/src/assets.rs`, status: accepted)

### CAMPAIGN
- **DEC-CAMPAIGN-001**: [untitled]  (`crates/sigint-core/src/campaign.rs`, status: )
- **DEC-CAMPAIGN-002**: Campaigns stored as UUID-keyed TEXT rows with optional completed_at  (`crates/sigint-store/src/campaigns.rs`, status: accepted)

### CLI
- **DEC-CLI-001**: scan command uses best-effort database persistence  (`crates/sigint-cli/src/scan.rs`, status: accepted)
- **DEC-CLI-002**: TUI task is awaited after scan; stdout printer is detached  (`crates/sigint-cli/src/scan.rs`, status: accepted)
- **DEC-CLI-003**: sessions subcommand uses best-effort database access, same as scan  (`crates/sigint-cli/src/sessions.rs`, status: accepted)
- **DEC-CLI-005**: report command accepts UUID prefix for session_id  (`crates/sigint-cli/src/report.rs`, status: accepted)
- **DEC-CLI-006**: serve subcommand uses serve_with_shutdown for clean Ctrl-C teardown  (`crates/sigint-cli/src/serve.rs`, status: accepted)

### CLI-DIFF
- **DEC-CLI-DIFF-001**: CLI diff uses direct DB access, not HTTP API  (`crates/sigint-cli/src/diff.rs`, status: accepted)

### CORE-REDACT
- **DEC-CORE-REDACT-001**: Single regex-based redactor in sigint-core, called at every persistence boundary  (`crates/sigint-core/src/redact.rs`, status: accepted)
- **DEC-CORE-REDACT-002**: Extend password-kv separator to match URL-encoded `=` (%3D / %3d)  (`crates/sigint-core/src/redact.rs`, status: accepted)

### DIFF
- **DEC-DIFF-001**: [untitled]  (`crates/sigint-tui/src/state.rs`, status: )

### DOCTOR
- **DEC-DOCTOR-001**: Synchronous checks for PATH/config, async HTTP for Ollama  (`crates/sigint-cli/src/doctor.rs`, status: accepted)

### E2E
- **DEC-E2E-001**: E2E tests use real Axum server on random port with in-memory SQLite  (`tests/e2e/src/lib.rs`, status: accepted)
- **DEC-E2E-002**: Session CRUD E2E tests cover empty list, 404 on missing, and bad UUID  (`tests/e2e/tests/sessions.rs`, status: accepted)
- **DEC-E2E-003**: Scan lifecycle E2E tests cover start, status, list, cancel, and session creation  (`tests/e2e/tests/scan_lifecycle.rs`, status: accepted)
- **DEC-E2E-004**: Scan E2E tests use MockProvider injected via ScanService::with_provider  (`tests/e2e/tests/scan_e2e.rs`, status: accepted)

### FINDING
- **DEC-FINDING-001**: Use a tool call (not text parsing) to extract structured findings  (`crates/sigint-tools/src/finding.rs`, status: accepted)
- **DEC-FINDING-002**: Phase 12B enrichment fields are optional in both schema and execute()  (`crates/sigint-tools/src/finding.rs`, status: accepted)

### LLM
- **DEC-LLM-001**: [untitled]  (`crates/sigint-core/src/config.rs`, status: )
- **DEC-LLM-002**: Tool-calling types use OpenAI-compatible JSON Schema format  (`crates/sigint-llm/src/types.rs`, status: accepted)
- **DEC-LLM-003**: MockProvider lives in sigint-llm so E2E tests can inject it without  (`crates/sigint-llm/src/mock.rs`, status: accepted)
- **DEC-LLM-004**: OpenAI-compatible provider with manual SSE parsing  (`crates/sigint-llm/src/openai.rs`, status: accepted)
- **DEC-LLM-005**: OpenAI arguments field deserialized as String then re-parsed  (`crates/sigint-llm/src/openai.rs`, status: accepted)
- **DEC-LLM-006**: Centralised provider factory for all LLM backends  (`crates/sigint-llm/src/factory.rs`, status: accepted)
- **DEC-LLM-007**: Accumulate tool_calls from all stream chunks, not just done=true  (`crates/sigint-agents/src/loop_engine.rs`, status: accepted)

### LOG
- **DEC-LOG-001**: sigint log renders chronological audit trail from scan_history  (`crates/sigint-cli/src/log.rs`, status: accepted)

### LOOP
- **DEC-LOOP-004**: Escalation detected via string marker in Strategist output  (`crates/sigint-agents/src/orchestrator.rs`, status: accepted)
- **DEC-LOOP-005**: Evidence linking via post-processing DB query after Executor  (`crates/sigint-store/src/scans.rs`, status: accepted)

### P13
- **DEC-P13-001**: Three-state scan status (Complete/TimedOut/Partial) provides tool-agnostic completion metadata  (`crates/sigint-tools/src/result.rs`, status: accepted)
- **DEC-P13-002**: 1MB default output cap prevents OOM from unbounded tool output while preserving enough data for meaningful analysis  (`crates/sigint-sandbox/src/command.rs`, status: accepted)
- **DEC-P13-003**: Regex-based text fallback when XML parsing fails  (`crates/sigint-tools/src/nmap.rs`, status: accepted)
- **DEC-P13-004**: Detect systemd-resolved stub (127.0.0.53) and resolve to upstream nameservers  (`crates/sigint-sandbox/src/command.rs`, status: accepted)
- **DEC-P13-005**: Best-effort structured parsing of gobuster output  (`crates/sigint-tools/src/gobuster.rs`, status: accepted)
- **DEC-P13-006**: Best-effort structured parsing of feroxbuster output  (`crates/sigint-tools/src/feroxbuster.rs`, status: accepted)
- **DEC-P13-007**: Best-effort structured parsing of nikto findings  (`crates/sigint-tools/src/nikto.rs`, status: accepted)

### P14
- **DEC-P14-001**: Strategist gains create_attack_plan tool  (`crates/sigint-agents/src/agents/strategist.rs`, status: accepted)

### P14-TOOLS
- **DEC-P14-TOOLS-001**: [untitled]  (`crates/sigint-core/src/config.rs`, status: )

### P15
- **DEC-P15-001**: SqlmapTool uses --batch for non-interactive LLM use  (`crates/sigint-tools/src/sqlmap.rs`, status: accepted)
- **DEC-P15-002**: ffuf uses JSON output mode (-of json) for reliable parsing  (`crates/sigint-tools/src/ffuf.rs`, status: accepted)
- **DEC-P15-003**: WhatwebTool uses SandboxProfile::recon() for fast passive fingerprinting  (`crates/sigint-tools/src/whatweb.rs`, status: accepted)
- **DEC-P15-004**: HydraTool uses SandboxProfile::bruteforce() with credential-specific args  (`crates/sigint-tools/src/hydra.rs`, status: accepted)
- **DEC-P15-005**: WpscanTool uses JSON output format for reliable structured parsing  (`crates/sigint-tools/src/wpscan.rs`, status: accepted)
- **DEC-P15-006**: TestsslTool uses SandboxProfile::recon() for passive TLS analysis  (`crates/sigint-tools/src/testssl.rs`, status: accepted)
- **DEC-P15-007**: HashcatTool uses SandboxProfile::offline() — no network, 60s timeout  (`crates/sigint-tools/src/hashcat.rs`, status: accepted)
- **DEC-P15-008**: MasscanTool uses SandboxProfile::nmap() for pasta networking  (`crates/sigint-tools/src/masscan.rs`, status: accepted)
- **DEC-P15-009**: TsharkTool uses SandboxProfile::nmap() for raw network access; JSON output for structured analysis  (`crates/sigint-tools/src/tshark.rs`, status: accepted)
- **DEC-P15-010**: Responder defaults to analyze-only mode (passive) for safety; active poisoning requires explicit opt-in  (`crates/sigint-tools/src/responder.rs`, status: accepted)
- **DEC-P15-011**: MsfconsoleTool uses SandboxProfile::web_scanner() (600s) for exploitation  (`crates/sigint-tools/src/msfconsole.rs`, status: accepted)
- **DEC-P15-012**: LinpeasTool uses SandboxProfile::offline() — no network, 60s timeout  (`crates/sigint-tools/src/linpeas.rs`, status: accepted)
- **DEC-P15-013**: Enum4linuxTool uses SandboxProfile::bruteforce() — pasta networking, 300s  (`crates/sigint-tools/src/enum4linux.rs`, status: accepted)
- **DEC-P15-014**: TrivyTool uses SandboxProfile::recon() — pasta networking, 60s timeout, Risk Low  (`crates/sigint-tools/src/trivy.rs`, status: accepted)
- **DEC-P15-015**: ScoutSuiteTool uses SandboxProfile::web_scanner() — pasta networking, 600s timeout, Risk Medium  (`crates/sigint-tools/src/scout_suite.rs`, status: accepted)
- **DEC-P15-016**: CloudsploitTool uses SandboxProfile::web_scanner() — pasta networking, 600s timeout, Risk Medium  (`crates/sigint-tools/src/cloudsploit.rs`, status: accepted)

### P19-EMBEDDED
- **DEC-P19-EMBEDDED-001**: EmbeddedProvider gated behind embedded-llm Cargo feature flag  (`crates/sigint-llm/src/embedded.rs`, status: accepted)
- **DEC-P19-EMBEDDED-002**: Load model fresh inside spawn_blocking rather than storing in struct  (`crates/sigint-llm/src/embedded.rs`, status: accepted)
- **DEC-P19-EMBEDDED-003**: Backend initialized once per process via OnceLock  (`crates/sigint-llm/src/embedded.rs`, status: accepted)
- **DEC-P19-EMBEDDED-004**: Emit heartbeat chunks during prefill to prevent stream idle timeout  (`crates/sigint-llm/src/embedded.rs`, status: accepted)

### P19-GGUF
- **DEC-P19-GGUF-001**: Pure-Rust GGUF header reader, no weight loading  (`crates/sigint-llm/src/gguf.rs`, status: accepted)

### P19-MODEL-CLI
- **DEC-P19-MODEL-CLI-001**: model pull uses blocking reqwest streaming without indicatif  (`crates/sigint-cli/src/model.rs`, status: accepted)

### P21-DB
- **DEC-P21-DB-001**: TuiApp owns Arc<Database>; queries run via spawn_blocking  (`crates/sigint-tui/src/app.rs`, status: accepted)

### P21-SETTINGS
- **DEC-P21-SETTINGS-001**: TuiSettings shadows Arc<Config> without mutating it  (`crates/sigint-tui/src/state.rs`, status: accepted)

### P21-STATE
- **DEC-P21-STATE-001**: AppState extended with multi-view fields; IO ownership stays in TuiApp  (`crates/sigint-tui/src/state.rs`, status: accepted)

### P21-UI
- **DEC-P21-UI-001**: ui/mod.rs owns chrome; per-view modules own content  (`crates/sigint-tui/src/ui/mod.rs`, status: accepted)

### P24
- **DEC-P24-001**: Fine-tune backend is an external shell-out command  (`crates/sigint-core/src/config.rs`, status: accepted)
- **DEC-P24-002**: Per-engagement opt-in for training data harvest  (`crates/sigint-store/src/sessions.rs`, status: accepted)
- **DEC-P24-003**: Evaluation methodology: live inference of both providers  (`crates/sigint-train/src/evaluate.rs`, status: accepted)
- **DEC-P24-004**: Promotion rewrites config.llm.model atomically via a CLI command  (`crates/sigint-cli/src/model.rs`, status: accepted)
- **DEC-P24-005**: Rollback is manual only (sigint model rollback)  (`crates/sigint-cli/src/model.rs`, status: accepted)
- **DEC-P24-007**: Modelfile ADAPTER directive requires a real LoRA adapter binary path  (`crates/sigint-train/src/modelfile.rs`, status: accepted)
- **DEC-P24-008**: Fine-tune output format is detected, not prescribed  (`crates/sigint-cli/src/model.rs`, status: accepted)

### P24-BUGFIX
- **DEC-P24-BUGFIX-001**: Process-level integration test for trainer exit-code propagation  (`crates/sigint-cli/tests/finetune_exit_code.rs`, status: accepted)

### P24-P1
- **DEC-P24-P1-001**: [source not found]

### P24-TEST
- **DEC-P24-TEST-001**: Integration tests for promote/rollback drive the real binary  (`crates/sigint-cli/tests/promote_rollback.rs`, status: accepted)
- **DEC-P24-TEST-002**: E2E smoke test uses in-memory DB + mock trainer + MockProvider  (`crates/sigint-train/tests/finetune_loop.rs`, status: accepted)

### P3
- **DEC-P3-001**: MemoryService is an optional field on Orchestrator  (`crates/sigint-agents/src/orchestrator.rs`, status: accepted)
- **DEC-P3-002**: fastembed always-on with all-MiniLM-L6-v2  (`crates/sigint-store/src/embeddings.rs`, status: accepted)
- **DEC-P3-003**: [untitled]  (`crates/sigint-cli/src/scan.rs`, status: )

### P3-TUI
- **DEC-P3-TUI-001**: AppState as pure event-driven state machine  (`crates/sigint-tui/src/state.rs`, status: accepted)
- **DEC-P3-TUI-002**: render() is a pure function of AppState with no side effects  (`crates/sigint-tui/src/ui/scan.rs`, status: accepted)
- **DEC-P3-TUI-003**: TuiApp separates terminal I/O from state; state lives in AppState  (`crates/sigint-tui/src/app.rs`, status: accepted)

### P6-APPROVAL
- **DEC-P6-APPROVAL-001**: PendingApproval held in AppState; approval responses emitted by app.rs  (`crates/sigint-tui/src/state.rs`, status: accepted)
- **DEC-P6-APPROVAL-002**: Approval bar occupies a conditional 1-row slot at the very bottom  (`crates/sigint-tui/src/ui/scan.rs`, status: accepted)

### PLUGIN
- **DEC-PLUGIN-001**: inventory crate for zero-boilerplate link-time tool registration  (`crates/sigint-plugin/src/lib.rs`, status: accepted)
- **DEC-PLUGIN-002**: `sigint plugin new` generates workspace member crates  (`crates/sigint-cli/src/plugin.rs`, status: accepted)
- **DEC-PLUGIN-003**: Orchestrator uses fn-pointer for prompt overrides to break the crate cycle  (`crates/sigint-agents/src/prompt_pack.rs`, status: accepted)

### RECON
- **DEC-RECON-001**: DNS module uses dig via SandboxProfile::recon() with spawn_blocking  (`crates/sigint-recon/src/dns.rs`, status: accepted)
- **DEC-RECON-002**: Port module uses nmap via SandboxProfile::nmap() (pasta networking)  (`crates/sigint-recon/src/port.rs`, status: accepted)
- **DEC-RECON-003**: Web module uses curl -sI (HEAD) via sandbox for HTTP fingerprinting  (`crates/sigint-recon/src/web.rs`, status: accepted)
- **DEC-RECON-004**: Cert module uses reqwest (not sandbox) to query crt.sh JSON API  (`crates/sigint-recon/src/cert.rs`, status: accepted)
- **DEC-RECON-005**: OSINT module uses whois via SandboxProfile::recon() with key parsing  (`crates/sigint-recon/src/osint.rs`, status: accepted)
- **DEC-RECON-006**: Correlator deduplicates by (kind, value) and enriches metadata with relationships  (`crates/sigint-recon/src/correlator.rs`, status: accepted)
- **DEC-RECON-007**: Change detector compares metadata JSON blobs as strings for simplicity  (`crates/sigint-recon/src/change.rs`, status: accepted)
- **DEC-RECON-008**: ReconEngine orchestrates modules sequentially with best-effort error handling  (`crates/sigint-recon/src/lib.rs`, status: accepted)

### RECON-VALIDATE
- **DEC-RECON-VALIDATE-001**: SSRF validator moved to sigint-core; sigint-recon keeps a shim  (`crates/sigint-recon/src/validate.rs`, status: accepted)

### REPORT
- **DEC-REPORT-001**: Report builder is pure Rust with no template engine dependency  (`crates/sigint-report/src/builder.rs`, status: accepted)
- **DEC-REPORT-002**: pulldown-cmark for Markdown-to-HTML rendering  (`crates/sigint-report/src/format.rs`, status: accepted)

### RESUME
- **DEC-RESUME-001**: Resume creates a new child session, then auto-diffs after scan completion  (`crates/sigint-cli/src/resume.rs`, status: accepted)

### SAND
- **DEC-SAND-001**: hakoniwa chosen over Docker for native Linux namespaces  (`crates/sigint-sandbox/src/lib.rs`, status: accepted)
- **DEC-SAND-002**: SandboxedCommand is synchronous; callers use spawn_blocking  (`crates/sigint-sandbox/src/lib.rs`, status: accepted)
- **DEC-SAND-003**: Capability detection via /proc and PATH walk at runtime  (`crates/sigint-sandbox/src/capability.rs`, status: accepted)
- **DEC-SAND-004**: Named profiles encode tool-class defaults (nmap, offline, web scanner, bruteforce)  (`crates/sigint-sandbox/src/profile.rs`, status: accepted)
- **DEC-SAND-005**: Integration tests run against real namespaces, no mocks  (`crates/sigint-sandbox/tests/sandbox_integration.rs`, status: accepted)
- **DEC-SAND-006**: Resolve bare commands to absolute paths before execve  (`crates/sigint-sandbox/src/command.rs`, status: accepted)

### STORE
- **DEC-STORE-001**: Embedded SQL migrations with integer version tracking  (`crates/sigint-store/src/migrations.rs`, status: accepted)
- **DEC-STORE-002**: ScanRecord stored as denormalized row — one row per tool invocation  (`crates/sigint-store/src/scans.rs`, status: accepted)
- **DEC-STORE-003**: Findings stored with severity TEXT and optional asset/evidence columns  (`crates/sigint-store/src/findings.rs`, status: accepted)

### TOOL
- **DEC-TOOL-001**: Crate-local ToolError for tool-specific failure modes  (`crates/sigint-tools/src/error.rs`, status: accepted)
- **DEC-TOOL-002**: ToolResult mirrors SandboxOutput with optional structured data  (`crates/sigint-tools/src/result.rs`, status: accepted)
- **DEC-TOOL-003**: Tool trait is the uniform interface for all sandboxed tool wrappers  (`crates/sigint-tools/src/lib.rs`, status: accepted)
- **DEC-TOOL-004**: NmapTool uses SandboxProfile::nmap() for pasta networking, -oX for structured output  (`crates/sigint-tools/src/nmap.rs`, status: accepted)
- **DEC-TOOL-005**: ShellTool uses a static allowlist and offline sandbox profile  (`crates/sigint-tools/src/shell.rs`, status: accepted)
- **DEC-TOOL-006**: NiktoTool uses SandboxProfile::web_scanner() for pasta networking  (`crates/sigint-tools/src/nikto.rs`, status: accepted)
- **DEC-TOOL-007**: NucleiTool uses SandboxProfile::web_scanner() for pasta networking  (`crates/sigint-tools/src/nuclei.rs`, status: accepted)
- **DEC-TOOL-008**: FeroxbusterTool uses SandboxProfile::bruteforce() for pasta networking  (`crates/sigint-tools/src/feroxbuster.rs`, status: accepted)

### TOOL-NUCLEI
- **DEC-TOOL-NUCLEI-002**: Allowlist template prefixes and system paths; reject URLs and arbitrary paths  (`crates/sigint-tools/src/nuclei.rs`, status: accepted)
- **DEC-TOOL-NUCLEI-003**: Reject path traversal sequences (`..`, `//`) in template paths; case-insensitive scheme check  (`crates/sigint-tools/src/nuclei.rs`, status: accepted)

### TOOL-SHELL-CANON
- **DEC-TOOL-SHELL-CANON-001**: Canonicalize paths before basename allowlist check to block symlink bypass  (`crates/sigint-tools/src/shell.rs`, status: accepted)

### TRAIN
- **DEC-TRAIN-001**: OpenAI chat-completion format for training JSONL  (`crates/sigint-train/src/lib.rs`, status: accepted)
- **DEC-TRAIN-002**: Extract one TrainingExample per successful ScanRecord tool call  (`crates/sigint-train/src/extract.rs`, status: accepted)
- **DEC-TRAIN-003**: One JSON object per line (JSONL) with no session_id field  (`crates/sigint-train/src/format.rs`, status: accepted)
- **DEC-TRAIN-004**: Session-based 80/20 split using session_id hash, not example index  (`crates/sigint-train/src/split.rs`, status: accepted)
- **DEC-TRAIN-005**: [source not found]
- **DEC-TRAIN-006**: Argument comparison uses exact string match on normalized JSON  (`crates/sigint-train/src/assess.rs`, status: accepted)
- **DEC-TRAIN-007**: Training output goes to ~/.local/share/sigint/training/ (XDG data home)  (`crates/sigint-cli/src/train.rs`, status: accepted)

### TRAIN-EXTRACT-REDACT
- **DEC-TRAIN-EXTRACT-REDACT-001**: Redact credentials from tool output before emitting training examples  (`crates/sigint-train/src/extract.rs`, status: accepted)

### TUI
- **DEC-TUI-002**: Redirect tracing output to a file when TUI is active  (`crates/sigint-cli/src/main.rs`, status: accepted)

### TUI-BUG
- **DEC-TUI-BUG-001**: TerminalGuard drop-guard ensures terminal is restored on all exit paths  (`crates/sigint-tui/src/app.rs`, status: accepted)
- **DEC-TUI-BUG-002**: Resize events consumed and redrawn immediately via the normal render cycle  (`crates/sigint-tui/src/app.rs`, status: accepted)

### WEB
- **DEC-WEB-001**: REST handlers are thin wrappers over store CRUD with no business logic  (`crates/sigint-web/src/routes.rs`, status: accepted)
- **DEC-WEB-002**: Same-origin fetch wrappers with no external HTTP client library  (`web/src/api.js`, status: accepted)
- **DEC-WEB-003**: Native WebSocket with exponential-backoff reconnect, no external library  (`web/src/ws.js`, status: accepted)
- **DEC-WEB-004**: Dashboard fetches session list only; per-session detail fetched on demand  (`web/src/components/Dashboard.js`, status: accepted)
- **DEC-WEB-005**: Client-side search filter over full session list  (`web/src/components/Sessions.js`, status: accepted)
- **DEC-WEB-006**: ScanView subscribes to the shared WS connection; events filtered by session  (`web/src/components/ScanView.js`, status: accepted)
- **DEC-WEB-007**: Assets page aggregates across sessions with client-side grouping  (`web/src/components/Assets.js`, status: accepted)
- **DEC-WEB-008**: Reports rendered server-side; browser downloads via Blob URL  (`web/src/components/Reports.js`, status: accepted)
- **DEC-WEB-009**: Hash-based routing with no router library; single WS connection shared globally  (`web/src/app.js`, status: accepted)
- **DEC-WEB-010**: Dashboard "Start Scan" form navigates to ScanView on success  (`web/src/components/Dashboard.js`, status: accepted)
- **DEC-WEB-011**: Approval modal uses externally-tagged Rust enum key detection  (`web/src/components/ScanView.js`, status: accepted)
- **DEC-WEB-012**: [source not found]
- **DEC-WEB-021**: Discriminated union for WebSocket events using `type` literal field  (`crates/sigint-web/frontend/src/types.ts`, status: accepted)
- **DEC-WEB-022**: Hash-based SPA routing via window.location.hash and hashchange event  (`crates/sigint-web/frontend/src/api.ts`, status: accepted)
- **DEC-WEB-023**: WebSocketManager singleton with auto-reconnect and subscribe/unsubscribe pattern  (`crates/sigint-web/frontend/src/ws.ts`, status: accepted)
- **DEC-WEB-025**: Sidebar uses CSS hover expansion (48px → 200px) with no JS state  (`crates/sigint-web/frontend/src/components/Sidebar.tsx`, status: accepted)
- **DEC-WEB-026**: TopBar carries WebSocket status badge and live scan indicator  (`crates/sigint-web/frontend/src/components/TopBar.tsx`, status: accepted)
- **DEC-WEB-027**: App shell uses hash router with useState + hashchange listener  (`crates/sigint-web/frontend/src/app.tsx`, status: accepted)
- **DEC-WEB-028**: DataTable generic over T with Column render prop for cell customisation  (`crates/sigint-web/frontend/src/components/DataTable.tsx`, status: accepted)
- **DEC-WEB-030**: SessionDetail uses parallel Promise.all fetch + tab state  (`crates/sigint-web/frontend/src/pages/SessionDetail.tsx`, status: accepted)
- **DEC-WEB-031**: NewScan encodes agent config fields into StartScanParams.options map  (`crates/sigint-web/frontend/src/pages/NewScan.tsx`, status: accepted)
- **DEC-WEB-032**: FindingsDetail uses client-side filter from session findings list  (`crates/sigint-web/frontend/src/pages/FindingsDetail.tsx`, status: accepted)
- **DEC-WEB-033**: AttackPlanView uses AttackStep records from scans API filtered by session  (`crates/sigint-web/frontend/src/pages/AttackPlanView.tsx`, status: accepted)
- **DEC-WEB-034**: ScanDiff uses client-initiated diff fetch after explicit Compare action  (`crates/sigint-web/frontend/src/pages/ScanDiff.tsx`, status: accepted)
- **DEC-WEB-035**: Settings page is read-only and sourced from /api/health + hardcoded defaults  (`crates/sigint-web/frontend/src/pages/Settings.tsx`, status: accepted)

### WEB-AUTH
- **DEC-WEB-AUTH-001**: Bearer token + shared secret for all REST and WebSocket endpoints  (`crates/sigint-web/src/lib.rs`, status: accepted)
- **DEC-WEB-AUTH-002**: Auto-generate and persist API key on first boot  (`crates/sigint-core/src/config.rs`, status: accepted)
- **DEC-WEB-AUTH-003**: WS auth via Authorization header OR ?token= query param OR  (`crates/sigint-web/src/auth.rs`, status: accepted)

### WEB-ERROR
- **DEC-WEB-ERROR-001**: Generic 500 body + server-side full error log (CSO Finding #8)  (`crates/sigint-web/src/routes.rs`, status: accepted)

### WEB-RATELIMIT
- **DEC-WEB-RATELIMIT-001**: Simple concurrent-scan count cap (vs token-bucket / sliding window) (CSO Finding #9)  (`crates/sigint-web/src/routes.rs`, status: superseded by DEC-WEB-RATELIMIT-002)
- **DEC-WEB-RATELIMIT-002**: Tokio Semaphore replaces check-then-act count check (TOCTOU fix)  (`crates/sigint-agents/src/scan_service.rs`, status: accepted)
